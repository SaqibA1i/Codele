import { HBox } from "../../styles/HBox";

const Navbar = () => {
  return (
    <HBox fontSize={70} style={{ fontFamily: "Dancing Script" }}>
      <p>Codele</p>
    </HBox>
  );
};

export default Navbar;
